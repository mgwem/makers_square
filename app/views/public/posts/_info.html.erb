<%= image_tag post.get_post_image(500, 500), class: "w-100" %>
<p class="mt-3">
  <span class="mr-2"><%= post.created_at.strftime('%Y/%m/%d') %></span>
  <!-- いいね機能 -->
  <%= render 'public/favorites/btn', post: post %>
  <span class="text-danger favorite-count"> <%= post.favorites.count %></span>
</p>
<p>
  <% if post.explanation.nil? %>
    <%= post.explanation %>
  <% else %>
    <%= safe_join(post.explanation.split("\n"), tag(:br)) %>
  <% end %>
</p>