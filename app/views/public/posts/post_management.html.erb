<div class="container mt-5">
  <div class="row">
    <div class="col-md-8 mx-auto">
      <h2>作品管理</h2>
      <div class="mb-4 text-right">
      <%= link_to "新規投稿", new_post_path, class: "btn btn-success" %>
      </div>
      <table class="table">
        <tr>
          <th></th>
          <th>作品タイトル</th>
          <th>公開ステータス</th>
          <th></th>
        </tr>
        <% @posts.each do |post| %>
        <tr>
          <td>
            <%= link_to member_post_path(post.member, post) do %>
              <%= image_tag post.get_post_image(50, 50) %>
            <% end %>
          </td>
          <td>
            <%= link_to member_post_path(post.member, post) do %>
              <%= post.title %>
            <% end %>
          </td>
          <td>
            <% if post.is_published == true %>
              <span class="text-danger">公開</span>
            <% elsif post.is_published  == false %>
              <span class="text-success">非公開</span>
            <% end %>
          </td>
          <td>
            <%= link_to "編集", edit_post_path(post), class: "btn btn-warning text-white" %>
          </td>
        <% end %>
        </tr>
      </table>
    </div>
  </div>
</div>